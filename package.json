{
  "name": "2025-11-21-ts-better-auth-infer-plugins",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run build:types && bun build index.ts --outdir dist --target node",
    "build:types": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "better-auth": "1.4.2-beta.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "5.9.3"
  }
}
